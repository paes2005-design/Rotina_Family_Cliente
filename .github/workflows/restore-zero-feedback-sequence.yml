name: Disabled temporary zero feedback patch
on:
  workflow_dispatch:
jobs:
  disabled:
    runs-on: ubuntu-latest
    steps:
      - run: echo 'Disabled. Final behavior is applied directly in the client.'
