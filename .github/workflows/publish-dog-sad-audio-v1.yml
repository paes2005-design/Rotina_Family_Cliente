name: Disabled temporary dog sad audio publisher
on:
  workflow_dispatch:
jobs:
  disabled:
    runs-on: ubuntu-latest
    steps:
      - run: echo 'Temporary publisher disabled; release is already applied directly.'
