const GOOGLE_TOKEN_URL = 'https://oauth2.googleapis.com/token';
const RULES_API = 'https://firebaserules.googleapis.com/v1';
const FIRESTORE_RULES_SOURCE = "rules_version = '2';\nservice cloud.firestore {\n  match /databases/{database}/documents {\n    function signedIn() {\n      return request.auth != null;\n    }\n\n    function rolePath() {\n      return /databases/$(database)/documents/authRoles/$(request.auth.uid);\n    }\n\n    function role() {\n      return get(rolePath()).data;\n    }\n\n    function activeRole() {\n      return signedIn() && exists(rolePath()) && role().ativo == true;\n    }\n\n    function isMaster() {\n      return activeRole() && role().papel == 'master';\n    }\n\n    function isFamilyAdmin() {\n      return activeRole() && role().papel == 'adm_familia';\n    }\n\n    function isGuestAdmin() {\n      return activeRole() && role().papel == 'adm_convidado';\n    }\n\n    function isParticipant() {\n      return activeRole() && role().papel == 'participante';\n    }\n\n    function myGroup() {\n      return activeRole() ? role().grupoId : '';\n    }\n\n    function myProfile() {\n      return isParticipant() ? role().perfilId : '';\n    }\n\n    function sameGroup(data) {\n      return activeRole() && data.grupoId == myGroup();\n    }\n\n    function participantOwn(data) {\n      return isParticipant()\n        && data.grupoId == myGroup()\n        && data.perfilId == myProfile();\n    }\n\n    function familyOperatorFor(data) {\n      return (isFamilyAdmin() || isGuestAdmin()) && data.grupoId == myGroup();\n    }\n\n    function guestPermission(key) {\n      return isGuestAdmin()\n        && role().permissoes is map\n        && role().permissoes[key] == true;\n    }\n\n    function taskIdentityUnchanged() {\n      return !request.resource.data.diff(resource.data).affectedKeys().hasAny([\n        'grupoId','perfilId','perfilNome','nome','diaSemana','horaSugeridaInicio',\n        'horaSugeridaFim','tempoLimite','pontosMaximos','justificativaObrigatoria',\n        'tarefaGrupoId'\n      ]);\n    }\n\n    function historyIdentityUnchanged() {\n      return !request.resource.data.diff(resource.data).affectedKeys().hasAny([\n        'grupoId','perfilId','perfilNome','tarefaId','tarefaGrupoId','data','nomeTarefa'\n      ]);\n    }\n\n    function alarmIdentityUnchanged() {\n      return !request.resource.data.diff(resource.data).affectedKeys().hasAny([\n        'grupoId','perfilId','tarefaId'\n      ]);\n    }\n\n    match /authRoles/{uid} {\n      allow read, write: if false;\n    }\n\n    match /administradores/{id} {\n      // ADM comum só precisa ler o próprio cadastro. A condição por uid também\n      // mantém compatibilidade com registros legados que ainda usam codigoCliente.\n      allow read: if isMaster()\n        || (activeRole() && (isFamilyAdmin() || isGuestAdmin()) && resource.data.uid == request.auth.uid);\n      allow create, update, delete: if isMaster();\n    }\n\n    match /perfis/{id} {\n      allow read: if isMaster()\n        || ((isFamilyAdmin() || isGuestAdmin()) && resource.data.grupoId == myGroup())\n        || participantOwn(resource.data);\n      allow create: if isMaster()\n        || (isFamilyAdmin() && request.resource.data.grupoId == myGroup())\n        || (guestPermission('participantesGerenciar') && request.resource.data.grupoId == myGroup());\n      allow update, delete: if isMaster()\n        || (isFamilyAdmin() && resource.data.grupoId == myGroup() && request.resource.data.grupoId == myGroup())\n        || (guestPermission('participantesGerenciar') && resource.data.grupoId == myGroup() && request.resource.data.grupoId == myGroup());\n    }\n\n    match /tarefas/{id} {\n      allow read: if isMaster() || familyOperatorFor(resource.data) || participantOwn(resource.data);\n      allow create: if isMaster()\n        || (isFamilyAdmin() && request.resource.data.grupoId == myGroup())\n        || (guestPermission('tarefasGerenciar') && request.resource.data.grupoId == myGroup());\n      allow update: if isMaster()\n        || (isFamilyAdmin() && resource.data.grupoId == myGroup() && request.resource.data.grupoId == myGroup())\n        || (guestPermission('tarefasGerenciar') && resource.data.grupoId == myGroup() && request.resource.data.grupoId == myGroup())\n        || (participantOwn(resource.data) && participantOwn(request.resource.data) && taskIdentityUnchanged());\n      allow delete: if isMaster()\n        || (isFamilyAdmin() && resource.data.grupoId == myGroup())\n        || (guestPermission('tarefasGerenciar') && resource.data.grupoId == myGroup());\n    }\n\n    match /historico/{id} {\n      allow read: if isMaster() || familyOperatorFor(resource.data) || participantOwn(resource.data);\n      allow create: if isMaster()\n        || ((isFamilyAdmin() || isGuestAdmin()) && request.resource.data.grupoId == myGroup())\n        || participantOwn(request.resource.data);\n      allow update: if isMaster()\n        || ((isFamilyAdmin() || isGuestAdmin()) && resource.data.grupoId == myGroup() && request.resource.data.grupoId == myGroup())\n        || (participantOwn(resource.data) && participantOwn(request.resource.data) && historyIdentityUnchanged());\n      allow delete: if isMaster() || (isFamilyAdmin() && resource.data.grupoId == myGroup());\n    }\n\n    match /execucoes/{id} {\n      allow read: if isMaster() || familyOperatorFor(resource.data) || participantOwn(resource.data);\n      allow create: if isMaster()\n        || ((isFamilyAdmin() || isGuestAdmin()) && request.resource.data.grupoId == myGroup())\n        || participantOwn(request.resource.data);\n      allow update: if isMaster()\n        || ((isFamilyAdmin() || isGuestAdmin()) && resource.data.grupoId == myGroup() && request.resource.data.grupoId == myGroup())\n        || (participantOwn(resource.data) && participantOwn(request.resource.data) && historyIdentityUnchanged());\n      allow delete: if isMaster() || (isFamilyAdmin() && resource.data.grupoId == myGroup());\n    }\n\n    match /recompensas/{id} {\n      allow read: if isMaster() || sameGroup(resource.data);\n      allow create: if isMaster()\n        || (isFamilyAdmin() && request.resource.data.grupoId == myGroup())\n        || (guestPermission('recompensasGerenciar') && request.resource.data.grupoId == myGroup());\n      allow update, delete: if isMaster()\n        || (isFamilyAdmin() && resource.data.grupoId == myGroup() && request.resource.data.grupoId == myGroup())\n        || (guestPermission('recompensasGerenciar') && resource.data.grupoId == myGroup() && request.resource.data.grupoId == myGroup());\n    }\n\n    match /resgates/{id} {\n      allow read: if isMaster()\n        || ((isFamilyAdmin() || isGuestAdmin()) && resource.data.grupoId == myGroup())\n        || participantOwn(resource.data);\n      allow create: if isMaster()\n        || ((isFamilyAdmin() || isGuestAdmin()) && request.resource.data.grupoId == myGroup())\n        || (participantOwn(request.resource.data) && request.resource.data.status == 'Pendente');\n      allow update: if isMaster()\n        || (isFamilyAdmin() && resource.data.grupoId == myGroup() && request.resource.data.grupoId == myGroup())\n        || (guestPermission('resgatesDecidir') && resource.data.grupoId == myGroup() && request.resource.data.grupoId == myGroup())\n        || (participantOwn(resource.data)\n            && participantOwn(request.resource.data)\n            && request.resource.data.diff(resource.data).affectedKeys().hasOnly(['pushClientePendente','clienteRetornoVistoEm']));\n      allow delete: if isMaster() || (isFamilyAdmin() && resource.data.grupoId == myGroup());\n    }\n\n    match /despertadores/{id} {\n      allow read: if isMaster() || familyOperatorFor(resource.data) || participantOwn(resource.data);\n      allow create: if isMaster()\n        || ((isFamilyAdmin() || isGuestAdmin()) && request.resource.data.grupoId == myGroup())\n        || participantOwn(request.resource.data);\n      allow update: if isMaster()\n        || ((isFamilyAdmin() || isGuestAdmin()) && resource.data.grupoId == myGroup() && request.resource.data.grupoId == myGroup())\n        || (participantOwn(resource.data) && participantOwn(request.resource.data) && alarmIdentityUnchanged());\n      allow delete: if isMaster()\n        || (isFamilyAdmin() && resource.data.grupoId == myGroup())\n        || (participantOwn(resource.data));\n    }\n\n    match /configGrupos/{groupId} {\n      allow read: if isMaster()\n        || (isFamilyAdmin() && groupId == myGroup())\n        || (isGuestAdmin() && groupId == myGroup())\n        || (isParticipant() && groupId == myGroup());\n      allow create, update: if isMaster() || (isFamilyAdmin() && groupId == myGroup());\n      allow delete: if isMaster();\n    }\n\n    match /estadoComercial/{groupId} {\n      allow read: if isMaster() || (isFamilyAdmin() && groupId == myGroup());\n      allow create, update, delete: if isMaster();\n    }\n\n    match /appLogs/{id} {\n      allow create: if isMaster()\n        || (activeRole() && request.resource.data.grupoId == myGroup());\n      allow read, update, delete: if isMaster();\n    }\n\n    match /appLogsSecure/{id} {\n      allow read: if isMaster();\n      allow create, update, delete: if false;\n    }\n\n    match /systemMigrations/{id} {\n      allow read: if isMaster();\n      allow write: if false;\n    }\n\n    match /systemMaintenance/{id} {\n      allow read: if isMaster();\n      allow write: if false;\n    }\n\n    match /{document=**} {\n      allow read, write: if isMaster();\n    }\n  }\n}\n";
let tokenCache = { value:'', expiresAt:0, email:'' };

const required=(value,name)=>{if(!value)throw new Error('Configuração obrigatória ausente: '+name);return value;};
function base64Url(bytes){let text='';for(const byte of bytes)text+=String.fromCharCode(byte);return btoa(text).replaceAll('+','-').replaceAll('/','_').replace(/=+$/g,'');}
function encodeJson(value){return base64Url(new TextEncoder().encode(JSON.stringify(value)));}
function pemBytes(pem){const normalized=String(pem||'').replaceAll('\\n','\n').replace(/-----BEGIN PRIVATE KEY-----/g,'').replace(/-----END PRIVATE KEY-----/g,'').replace(/\s/g,'');const binary=atob(normalized);return Uint8Array.from(binary,c=>c.charCodeAt(0));}
function credentials(env){const value=JSON.parse(required(env.GOOGLE_SERVICE_ACCOUNT_JSON,'GOOGLE_SERVICE_ACCOUNT_JSON'));required(value.client_email,'client_email');required(value.private_key,'private_key');return value;}
async function googleToken(env,now=new Date()){
const c=credentials(env);if(tokenCache.value&&tokenCache.email===c.client_email&&tokenCache.expiresAt>now.getTime()+60000)return tokenCache.value;
const iat=Math.floor(now.getTime()/1000);
const unsigned=encodeJson({alg:'RS256',typ:'JWT'})+'.'+encodeJson({iss:c.client_email,sub:c.client_email,scope:'https://www.googleapis.com/auth/cloud-platform',aud:GOOGLE_TOKEN_URL,iat,exp:iat+3600});
const key=await crypto.subtle.importKey('pkcs8',pemBytes(c.private_key),{name:'RSASSA-PKCS1-v1_5',hash:'SHA-256'},false,['sign']);
const signature=await crypto.subtle.sign('RSASSA-PKCS1-v1_5',key,new TextEncoder().encode(unsigned));
const assertion=unsigned+'.'+base64Url(new Uint8Array(signature));
const response=await fetch(GOOGLE_TOKEN_URL,{method:'POST',headers:{'content-type':'application/x-www-form-urlencoded'},body:new URLSearchParams({grant_type:'urn:ietf:params:oauth-type:jwt-bearer',assertion})});
if(!response.ok){
const retry=await fetch(GOOGLE_TOKEN_URL,{method:'POST',headers:{'content-type':'application/x-www-form-urlencoded'},body:new URLSearchParams({grant_type:'urn:ietf:params:oauth:grant-type:jwt-bearer',assertion})});
const retryBody=await retry.json().catch(()=>({}));
if(!retry.ok||!retryBody.access_token)throw new Error('OAuth Google recusado ('+retry.status+'): '+String(retryBody.error_description||retryBody.error||'sem detalhe').slice(0,180));
tokenCache={value:retryBody.access_token,email:c.client_email,expiresAt:now.getTime()+Number(retryBody.expires_in||3600)*1000};return tokenCache.value;
}
const body=await response.json().catch(()=>({}));if(!body.access_token)throw new Error('OAuth Google não retornou token.');
tokenCache={value:body.access_token,email:c.client_email,expiresAt:now.getTime()+Number(body.expires_in||3600)*1000};return tokenCache.value;
}
async function api(env,url,options={},now=new Date()){
const response=await fetch(url,{...options,headers:{authorization:'Bearer '+await googleToken(env,now),...(options.headers||{})}});
const body=await response.json().catch(()=>({}));return {response,body};
}
function apiError(label,result){const message=String(result.body?.error?.message||result.body?.error||'sem detalhe').replace(/\s+/g,' ').slice(0,300);return new Error(label+' recusado ('+result.response.status+'): '+message);}

export async function deployFirestoreRulesV1(env,now=new Date()){
const projectId=required(env.FIREBASE_PROJECT_ID,'FIREBASE_PROJECT_ID');
const project='projects/'+projectId;
const created=await api(env,RULES_API+'/'+project+'/rulesets',{method:'POST',headers:{'content-type':'application/json'},body:JSON.stringify({source:{files:[{name:'firestore.rules',content:FIRESTORE_RULES_SOURCE}]}})},now);
if(!created.response.ok||!created.body?.name)throw apiError('Criação do ruleset',created);
const rulesetName=String(created.body.name);
const releaseName=project+'/releases/cloud.firestore';
const current=await api(env,RULES_API+'/'+releaseName,{},now);
let released;
if(current.response.status===404){
released=await api(env,RULES_API+'/'+project+'/releases',{method:'POST',headers:{'content-type':'application/json'},body:JSON.stringify({name:releaseName,rulesetName})},now);
}else{
if(!current.response.ok)throw apiError('Leitura da release atual',current);
released=await api(env,RULES_API+'/'+releaseName,{method:'PATCH',headers:{'content-type':'application/json'},body:JSON.stringify({release:{name:releaseName,rulesetName},updateMask:'rulesetName'})},now);
}
if(!released.response.ok)throw apiError('Publicação da release',released);
const verified=await api(env,RULES_API+'/'+releaseName,{},now);
if(!verified.response.ok)throw apiError('Verificação da release',verified);
const activeRuleset=String(verified.body?.rulesetName||'');
return {ok:true,rulesetName,releaseName,activeRuleset,releaseMatches:activeRuleset===rulesetName,updateTime:String(verified.body?.updateTime||'')};
}
